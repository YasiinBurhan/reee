--- app/src/main/java/com/equinox/virtual/viewmodel/BlackBoxViewModel.kt
+++ app/src/main/java/com/equinox/virtual/viewmodel/BlackBoxViewModel.kt
@@ -365,14 +365,6 @@
         viewModelScope.launch(Dispatchers.IO) {
             try {
                 val userId = _currentUserId.value
-                if (isModMode) {
-                    if (injected) {
-                        Log.d("BlackBoxViewModel", "Mod injection successful for $packageName")
-                        
-                    } else {
-                        Log.e("BlackBoxViewModel", "Mod injection failed for $packageName")
-                    }
-                }
                 val success = BlackBoxCore.get().launchApk(packageName, userId)
                 val modeLabel = "Mode Normal"
                 if (success) {
